apiVersion: apps/v1
kind: Deployment
metadata:
  name: boards-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: boards-mongo
  template:
    metadata:
      labels:
        app: boards-mongo
    spec:
      volumes:
        - name: mongo-storage
          persistentVolumeClaim:
            claimName: boards-db-pvc

      containers:
        - name: boards-mongo
          image: mongo
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: boards-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi

---
apiVersion: v1
kind: Service
metadata:
  name: boards-mongo-srv
spec:
  selector:
    app: boards-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
