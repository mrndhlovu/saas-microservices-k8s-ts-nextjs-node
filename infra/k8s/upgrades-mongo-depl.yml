apiVersion: apps/v1
kind: Deployment
metadata:
  name: upgrades-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upgrades-mongo
  template:
    metadata:
      labels:
        app: upgrades-mongo
    spec:
      containers:
        - name: upgrades-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: upgrades-mongo-srv
spec:
  selector:
    app: upgrades-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
